{
  "name": "pocketbase-mcp",
  "version": "1.1.0",
  "main": "build/index.js",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node scripts/smoke.mjs --contract-only",
    "test:integration": "node scripts/smoke.mjs",
    "build": "tsc && chmod +x build/index.js",
    "smoke": "node scripts/smoke.mjs"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "pocketbase"
  ],
  "author": "",
  "license": "ISC",
  "description": "MCP server for interacting with a PocketBase instance (records, collections, files, logs, crons and migrations).",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "pocketbase": "^0.28.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
